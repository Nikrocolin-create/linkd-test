# Вопросы к заметке (3) 27-6-26

Темы: транзакции Hibernate, flush, интеграционные тесты, REST-семантика, CDC.

## По материалу заметки

1. Почему `ALTER TABLE users ADD CONSTRAINT uq_users_email UNIQUE (email)` может упасть на существующей таблице и какую блокировку он берёт?
Если в таблице уже присутствуют неуникальные айди, блокировка ACCESS EXCLUSIVE
Как добавить такое ограничение с минимальным простоем (`CREATE UNIQUE INDEX CONCURRENTLY` + `ADD CONSTRAINT ... USING INDEX`)?
Не знаю
2. Объясните механику из заметки: почему исключение unique violation не попадает в `try-catch` внутри `@Transactional`-метода? 
??
Что такое first-level cache и отложенный flush?
First level cache - кэш внутри транзакции. Отложенный flush - это когда все изменения происходят по умолчанию в конце метода транзакции, даже если save не был вызван
3. Как работает `FlushModeType.AUTO` в Hibernate: в какие моменты flush происходит сам (перед запросом, при commit) и почему `save()` часто не даёт SQL немедленно?
Предыдущий вопрос
4. Что именно делает `repository.flush()` и почему это «костыль»?
выгружает все изменения в скл
Какие альтернативы поймать нарушение уникальности: предварительный `SELECT`, `saveAndFlush()`, ловля `DataIntegrityViolationException` на уровне выше, ретрай с новым кодом?
Все так и есть, не слышал про saveAndFlush(), но предполагаю что так выполнит вставку до конца метода и так можно будет поймать DataIntegrityViolationException

5. Почему проверка «`SELECT` есть ли такой short_code, потом `INSERT`» не защищает от гонок? 
Потому что другая транзакция может вставить новую запись до INSERT
Как ограничение в БД + обработка ошибки дают корректность там, где проверка в коде — нет?
При вставке в бд проверяется условие, вставка двух одинаковых кодов невозможно

6. Как Spring транслирует `ConstraintViolationException` из JDBC в `DataIntegrityViolationException`?
Даже не понимаю вопроса
Где это происходит и как отличить нарушение уникальности от нарушения FK?
Ну фк тебе даст, когда не существует записи в связанной таблице. 

7. Что делает `@SpringBootTest(webEnvironment = RANDOM_PORT)`: поднимается ли реальный сервер, чем это отличается от `MOCK` и `DEFINED_PORT`, 
Поднимается тестовый сервер, который все равно занимает порт, дефайнд и рандом по определению разный
и когда достаточно `@WebMvcTest`?
Хз
8. Чем `TestRestTemplate` отличается от `RestTemplate` и от `WebTestClient`? Как он получает порт запущенного сервера и почему не бросает исключение на 4xx/5xx?
Да хз
9. Разберите паттерн Location header: что делает `ServletUriComponentsBuilder.fromCurrentRequest().path("/{id}").buildAndExpand(id)`,
Location дает либо язык, либо время(часовой пояс, сдвиг, time zone)
почему для POST-создания правильно вернуть `201 Created` с `Location`, и какие проблемы возникают за reverse proxy (`X-Forwarded-*`, `ForwardedHeaderFilter`)?
   `201 Created` с `Location` чтобы узнать точное время
10. Как реализовать batch-удаление протухших ссылок: `@Scheduled` + пакетное `DELETE ... WHERE expires_at < now() LIMIT n` в цикле,
Навесить аннотацию @Scheduled, с крон выражением из application.yaml, EnableScheduling на инстанс 
почему не стоит удалять всё одним запросом,
Чем больше операция, тем больше откат если будет ошибка, плюс нельзя будет распараллелить
и как это делать при нескольких инстансах (блокировка, ShedLock)?
Блокировка на взятые записи, для каждого инстанса (Хорошо бы реализовать)

## Дополняющие вопросы (для полноты картины)

11. Что такое CDC (Change Data Capture)? 
При изменении данных отправляются сообщения.
Как устроена связка Debezium + Kafka для потока изменений из PostgreSQL (logical replication, WAL, slot) и из MongoDB (change streams / oplog)?
Не знаю, знаю что Debezium + Kafka используется для транзакц оутбокс, 
12. Какие гарантии даёт CDC — at-least-once или exactly-once? 
Ну по смыслу названию.
Как получатель делает обработку идемпотентной и зачем нужен transactional outbox вместо «двойной записи» в БД и в очередь?
Определяет по ключу. transactional outbox чтобы иметь общую транзакцию для бд и кафка
13. Как ведут себя уровни изоляции в PostgreSQL (`READ COMMITTED` по умолчанию, `REPEATABLE READ`, `SERIALIZABLE`) и когда в проекте реально нужен уровень выше дефолтного?

14. Почему `@Transactional` не работает при вызове метода изнутри того же бина (self-invocation) и на приватных методах? Как работает проксирование в Spring AOP?

15. Как поднять реальную БД в интеграционных тестах через Testcontainers, что даёт `@ServiceConnection`/`@DynamicPropertySource` и как обеспечить чистое состояние данных между тестами (`@Transactional`-откат vs очистка схемы)?
