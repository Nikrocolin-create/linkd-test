# Вопросы к заметке (1) 23-6-26

Темы: валидация, обработка ошибок, ProblemDetail, конфигурация, редиректы.

## По материалу заметки

1. Что делает аннотация `@URL` и из какой она библиотеки? 
Чем она отличается от `@Pattern` с регуляркой для URL и какие подвохи есть в её проверке (например, пройдёт ли `javascript:alert(1)` или `http://localhost`)?
Аннотация url проверяет, является ли переданная строка валидным урл
Паттерн позволяет указать любой регекс 

2. Как работает `@ControllerAdvice`: на какие контроллеры он распространяется по умолчанию, как ограничить его область действия и чем он отличается от `@RestControllerAdvice`?
Позволяет перехватывать исключения из контроллеров, отличает от рест контроллер тем, что не оборачивает ответы в ResponseBody. 
По умолчанию перехватывает исключения из коnтроллеров в одном пакете, ограничить можно разбив котнроллеры по разным пакетам

3. Когда именно Spring бросает `MethodArgumentNotValidException`, а когда — `ConstraintViolationException` и `HandlerMethodValidationException`? Что нужно добавить, чтобы валидация вообще сработала на теле запроса?
Для того чтобы сработала нужно добавить @Valid рядом с параметром -> MethodArgumentNotValidException. ConstraintViolationException ->  `@RequestParam`, `@PathVariable`
@Validated ставиться на класс в определении метода которого мы ставим @NotNull etc
4. Как из `MethodArgumentNotValidException` достать список всех ошибок полей (`BindingResult`) и превратить его в понятный ответ с картой `поле → сообщение`?
не помню метод, но там будет структура мап с полем и сообщением, можно сделать через ProblemDetail

5. Что такое `ProblemDetail` и стандарт RFC 7807/9457? Какие поля обязательны (`type`, `title`, `status`, `detail`, `instance`) и как добавить свои через `setProperty()`?
Это класс, который позволяет отправлять данные об ошибки и содержит поля type title status detail instance. Можно добавить свои через setProperty('name','value')

6. Чем `ProblemDetail.forStatusAndDetail(...)` отличается от наследования `ResponseEntityExceptionHandler`? Что даёт свойство `spring.mvc.problemdetails.enabled=true`?
не знаю

7. Как работает `@ConfigurationProperties(prefix = "mail")`: как связываются `hostName` в классе и ключ в `application.yml`, что нужно для включения биндинга (`@EnableConfigurationProperties` / `@ConfigurationPropertiesScan`), и почему подход лучше россыпи `@Value`?
чтобы @ConfigurationProperties сработала объект должен быть бином
Находит в application.yaml префик mail, а все поля под этим префиксом мапятся на поля класса над которым объявлена аннотация, снейк кейс мапиться в кэмел кейс если необходимо. Позволяет централизованно мапить переменные, а так же привязка происходит раньше чем @Value

8. Можно ли сделать класс с `@ConfigurationProperties` иммутабельным (record / constructor binding)? Как навесить на него валидацию (`@Validated`) и что произойдёт при старте, если значение не пройдёт проверку?
Да, можно. Валидатед наверное можно, тогда будет ошибка на старте 

9. Почему для редиректа короткой ссылки выбран `HttpStatus.FOUND` (302), а не 301 или 307/308? Как выбор кода влияет на кэширование в браузере и на возможность считать клики?
Хрен его знает. 301 кешируется браузером и будет постоянно перенаправлять

10. Что делает `ResponseEntity.status(...).location(URI.create(longUrl)).build()` и какие риски несёт подстановка пользовательского URL в заголовок `Location` (open redirect, CRLF-инъекция, невалидный URI)?
В теории можно переправить на вредоносный сайт

 
## Дополняющие вопросы (для полноты картины)

11. Как отдать единый формат ошибки и для валидации, и для 404, и для необработанного `Exception`, не дублируя код в каждом хендлере?
Передать несколько ошибок в аннотацию ExceptionHandler()
12. Что вернуть, если пользователь прислал невалидный JSON (`HttpMessageNotReadableException`) — 400 или 422? Как это согласуется с ProblemDetail?
422 unprocessable entity потому что ошибки в синтаксисе
13. Как локализовать сообщения валидации (`messages.properties`, `MessageSource`) и подставлять в них параметры?
I18n создать файлы, и передавать в запросе язык eng/ru/it
14. Как не «протечь» стектрейсом и внутренними деталями в тело ответа в продакшене (`server.error.include-stacktrace`, логирование vs ответ клиенту)?
Обычно считается что отправлять стактрейс клиенту плохо, так что лучше для каждого исключения подменять сообщение об ошибке, при это в логе стактрейс остается
15. Как протестировать `@ControllerAdvice`: что даёт `@WebMvcTest` + `MockMvc` и как проверить, что тело ответа — корректный `application/problem+json`?
В тестах мы вешаем @WebMvcTest на класс тестов потом берем объект?, mvc.perform и отправляем наш реквест, как будто работаем с настоящим приложением, нам вернется ответ, и мы можем его проверить