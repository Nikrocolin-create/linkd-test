services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: linkd
      POSTGRES_USER: linkd
      POSTGRES_PASSWORD: linkd
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
